@model IEnumerable<Web_Proje.Models.AspNetUserRoles>

@{
    ViewBag.Title = "Index";
}

<h2>Rol Tanımlama</h2>

<p>
    @Html.ActionLink("Yeni Rol Tanımla", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoleId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoleId)
        </td>
        <td>
            @Html.ActionLink("Düzenle", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Ayrıntılar", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Sil", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
